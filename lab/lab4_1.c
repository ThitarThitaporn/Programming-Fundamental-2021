#include<stdio.h>
#include<math.h>
int main(){
    float length1,length2,length3,area;
    scanf_s("%f %f %f",&length1,&length2,&length3);
    area= (length1+length2+length3)/2;

    if
    printf(" %f %f %f",length1,length2,length3);
}